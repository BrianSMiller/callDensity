% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/callDensitySupport.R
\name{plotSNRHistogram}
\alias{plotSNRHistogram}
\title{Plot SNR histogram by observer}
\usage{
plotSNRHistogram(
  df,
  obs_labels,
  snr_prefix = "snr_observer",
  snr_lims = NULL,
  binwidth = 0.5,
  title = "SNR distribution by detector",
  ...
)
}
\arguments{
\item{df}{A data frame containing SNR observer columns.}

\item{obs_labels}{Named character vector mapping numeric observer indices
(as character) to display labels.}

\item{snr_prefix}{Either a single character string pattern identifying SNR
columns, or a character vector of explicit column names. Defaults to
\code{"snr_observer"}.}

\item{snr_lims}{Numeric vector of length 2 giving x-axis limits. If
\code{NULL} (default), limits are computed from the data using
\code{\link{computeSNRLims}}.}

\item{binwidth}{Histogram bin width in dB. Defaults to \code{0.5}.}

\item{title}{Plot title. Defaults to \code{"SNR distribution by detector"}.}

\item{...}{Additional ggplot2 layers (e.g. scales, themes, annotations)
passed to the plot via \code{+}.}
}
\value{
A \code{ggplot} object (invisibly).
}
\description{
Pivots SNR observer columns to long format and produces a faceted histogram
of SNR distributions, one panel per observer.
}
\examples{
\dontrun{
plotSNRHistogram(ap, obs_labels,
  title = "Adjudicated subset: SNR distribution by detector")

# Shared limits with extra layer
lims <- computeSNRLims(d)
plotSNRHistogram(ap, obs_labels, snr_lims = lims,
  title = "Adjudicated subset: SNR distribution by detector",
  ggplot2::scale_fill_brewer(palette = "Dark2"))
plotSNRHistogram(d, obs_labels, snr_lims = lims,
  title = "Full dataset: SNR distribution by detector",
  ggplot2::scale_fill_brewer(palette = "Dark2"))
}

}
