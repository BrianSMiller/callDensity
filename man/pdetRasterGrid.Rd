% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/callDensitySupport.R
\name{pdetRasterGrid}
\alias{pdetRasterGrid}
\title{Resample (range, azimuth) cells onto a fine square Cartesian grid}
\usage{
pdetRasterGrid(long, rangeStep, angleStep, rasterRes = 400)
}
\arguments{
\item{long}{Data.frame from \code{pdetToLong}.}

\item{rangeStep}{Range bin width (km).}

\item{angleStep}{Azimuth bin width (degrees).}

\item{rasterRes}{Number of pixels along each axis of the square output
grid.}
}
\value{
A data.frame with columns \code{x}, \code{y}, \code{pDet}, one row
per pixel (pixels outside the sampled disc have \code{pDet = NA}).
}
\description{
Nearest-neighbour resampling, not interpolation: every output pixel takes
the value of its nearest (range, azimuth) sample. Assumes a constant
range step and constant angle step (true of \code{pDetInArea}'s output),
which is what makes the nearest-cell lookup a closed-form index
calculation rather than a search.
}
\keyword{internal}
