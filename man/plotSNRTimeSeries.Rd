% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/callDensitySupport.R
\name{plotSNRTimeSeries}
\alias{plotSNRTimeSeries}
\title{Plot SNR time series for the adjudicated subset}
\usage{
plotSNRTimeSeries(
  ap,
  obs_labels,
  snr_prefix = "snr_observer",
  time_col = "i",
  snr_lims = NULL,
  title = "Adjudicated subset: SNR time series by detector",
  ...
)
}
\arguments{
\item{ap}{A data frame containing SNR observer columns and a time index column.}

\item{obs_labels}{Named character vector mapping numeric observer indices
(as character) to display labels.}

\item{snr_prefix}{Either a single character string pattern identifying SNR
columns, or a character vector of explicit column names. Defaults to
\code{"snr_observer"}.}

\item{time_col}{Name of the time index column. Defaults to \code{"i"}.}

\item{snr_lims}{Numeric vector of length 2 giving y-axis limits. If
\code{NULL} (default), limits are computed from the data using
\code{\link{computeSNRLims}}.}

\item{title}{Plot title. Defaults to
\code{"Adjudicated subset: SNR time series by detector"}.}

\item{...}{Additional ggplot2 layers (e.g. scales, themes, annotations)
passed to the plot via \code{+}.}
}
\value{
A \code{ggplot} object (invisibly).
}
\description{
Pivots SNR observer columns to long format and produces a scatter plot of
SNR against a time index column, coloured by observer.
}
\examples{
\dontrun{
plotSNRTimeSeries(ap, obs_labels)

# Shared limits with extra layer
lims <- computeSNRLims(d)
plotSNRTimeSeries(ap, obs_labels, snr_lims = lims,
  ggplot2::theme(legend.position = "bottom"))
}

}
