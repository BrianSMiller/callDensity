% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/showDetFun.R
\name{clippedDensity}
\alias{clippedDensity}
\title{Kernel density estimate pre-clipped to a shared x-range}
\usage{
clippedDensity(x, xlim)
}
\arguments{
\item{x}{Numeric vector.}

\item{xlim}{Length-2 numeric vector, the shared panel x-range.}
}
\value{
A data.frame with columns \code{SNR} and \code{y}.
}
\description{
Passing \code{from}/\code{to} directly to \code{density()} keeps the
curve within the shared panel x-limits from the start, rather than
computing the usual wider KDE (which by default extends \code{cut}
bandwidths past the data) and letting ggplot2 silently drop and warn
about the clipped tails once \code{scale_x_continuous(limits=)} is
applied.
}
\keyword{internal}
