% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nlFromDetections.R
\name{predictSampledNL}
\alias{predictSampledNL}
\title{Predict the mean noise level you would measure at detections}
\usage{
predictSampledNL(
  mu,
  sigma,
  detFun,
  SL,
  TL,
  truncationDistance = max(TL[[1]]),
  nNodes = 121,
  ...
)
}
\arguments{
\item{mu}{Candidate true mean noise level in dB.}

\item{sigma}{Standard deviation of the true noise distribution in dB.}

\item{detFun}{Detection function. See \code{pDetGivenNL}.}

\item{SL}{Source level distribution, with elements named mean and sd.}

\item{TL}{Transmission loss table. See \code{pDetGivenNL}.}

\item{truncationDistance}{Scalar or one value per transect, in metres.}

\item{nNodes}{Number of nodes used to integrate over the noise distribution.}

\item{...}{Passed to \code{pDetGivenNL}, e.g. \code{binWidth}.}
}
\value{
Scalar. Always less than or equal to \code{mu}.
}
\description{
Given a candidate true noise distribution, predict the mean of the noise
levels that would be measured at detected calls. Those measurements are
biased low, because a quiet period gives a high SNR and a high SNR gives a
detection, so detections over-represent quiet periods.
}
\details{
The weighting is \code{pDetGivenNL}: if the chance of detection at 80 dB is 2.5
times the chance at 84 dB, then 80 dB periods appear among the detections
2.5 times more often than they occur.
}
