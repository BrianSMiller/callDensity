# Split an SNRinfo data.frame into detected/missed SNR vectors

Split an SNRinfo data.frame into detected/missed SNR vectors

## Usage

``` r
detMissSNR(obs)
```

## Arguments

- obs:

  Data.frame with columns `SNR` and `Detected`.

## Value

A list with elements `det` and `miss`.
